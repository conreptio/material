rootProject.name = 'natrolite'

include 'natrolite-api'
include 'natrolite-core'
include 'natrolite-utils'

rootProject.children.each {
    assert it.projectDir.isDirectory()
    assert it.buildFile.isFile()
}