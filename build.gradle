plugins {
    id 'net.minecrell.licenser' version '0.3' apply false
}

allprojects {
    group = 'org.natrolite'
    version = '1.0-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.5'
}