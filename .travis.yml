dist: trusty
sudo: false

language: java

jdk:
  - openjdk8
  - oraclejdk8
  - oraclejdk9

script: ./gradlew build checkstyle -Dorg.gradle.daemon=false

matrix:
  fast_finish: true
  allow_failures:
    - jdk: oraclejdk9

branches:
  except:
    - gh-pages

notifications:
  email: false
